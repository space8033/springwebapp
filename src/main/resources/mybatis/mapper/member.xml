<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mycompany.springwebapp.dao.Ch13MemberDao">
	<insert id="insert" parameterType="Member">
		insert into member(mid, mname, mpassword, menable, mrole, memail)
		values(#{mid}, #{mname}, #{mpassword}, 1, 'ROLE_USER', #{memail})
	</insert>
	
	<select id="selectByMid" parameterType="string" resultType="Member">
		select mid, mname, mpassword, menable, mrole, memail
		from member
		where mid = #{mid}
	</select>
	
	<update id="update" parameterType="Member">
		update member set memail = #{memail}, mpassword = #{mpassword}
		where mid = #{mid}
	</update>
</mapper>